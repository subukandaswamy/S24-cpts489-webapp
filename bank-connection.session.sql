CREATE TABLE USER (
    name varchar(255),
    password varchar(255),
    balance float
);

-- @block
INSERT INTO USER (name, password, balance) VALUES
('subu','1234',1000);

-- @block
INSERT INTO USER (name, password, balance) VALUES
('tim','12345',1000000);

